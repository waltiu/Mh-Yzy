const state = {
  addPipeInfo: {}
}

const mutations = {
  setPipeInfo (state, status) {
    state.addPipeInfo = status
  }
}

const actions = {

}

export default {
  state,
  mutations,
  actions
}
